1. create and fill `.env` file using `.env.example` template
2. first run
```bash
python3 -m venv env
source env/bin/activate
pip install -r requirements.txt
python main.py
```
3. update
```bash
./regenerate
```
